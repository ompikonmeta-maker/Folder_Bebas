# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Status: placeholder.** This repository is currently empty. The sections below are a
> skeleton to be filled in once code exists. Re-run `/init` after adding source files so
> Claude can document the real commands and architecture. Delete any section that stays
> empty rather than leaving it blank.

## Commands

<!-- Fill in once tooling exists. Examples of what belongs here:
- Build:        e.g. `make build` / `npm run build`
- Run / dev:    e.g. `npm run dev`
- Lint:         e.g. `npm run lint`
- Test (all):   e.g. `npm test`
- Test (single):e.g. `npm test -- path/to/test`
-->

## Architecture

<!-- Fill in once code exists. Focus on the big picture that requires reading multiple
files to understand: the main modules and how they interact, data flow, key entry points,
and any non-obvious conventions. Skip anything easily discovered by listing files. -->
